import SectionTitle from "../sectionTitle"

const ContactText = () => {
    return (
        <div className="contact-text">
            <SectionTitle text="Contact"/>
            <p>Ready to tell your story? Call me on 07956542404, or send me a message using the form below.</p>
        </div>
    )
}

export default ContactText
